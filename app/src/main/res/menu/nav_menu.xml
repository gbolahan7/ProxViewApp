<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/buttonHome"
            android:title="Home"
            android:icon="@drawable/ic_home"/>

        <item android:id="@+id/buttonLocation"
            android:title="Saved Locations"
            android:icon="@drawable/ic_location"/>

        <item android:id="@+id/buttonSetting"
            android:title="Settings"
            android:icon="@drawable/ic_settings"/>

    </group>

    <item android:title="App">
        <menu>
            <item android:id="@+id/buttonHelp"
                android:title="Help"
                android:icon="@drawable/ic_help"/>

            <item android:id="@+id/buttonAbout"
                android:title="@string/menu_activity"
                android:icon="@drawable/ic_about"/>
        </menu>
    </item>
</menu>